//parameters are variables that act as place holders for the values that are to be input to a function when it is called. 
// that means that wehn this functino is called we're going to pass data into the function. 

//another function with paramers
function addNumbers(a,b){
    result = a + b;
    console.log(result);
}

addNumbers(2,2); //  outputs 4


//another function example:
function subtractNumbers(a,b){
    result = a - b;
    console.log(result);
}

subtractNumbers(2,2); //  outputs 0

