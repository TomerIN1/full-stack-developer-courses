// use the inequality operator != to check if values are equal. 

function testEqual(val){
    if (a != b) { //the inequality operator is strict equal sign (!=).
        return "Equal";
    }
    return "Not Equal";
}

console.log(testEqual(10, 9));

