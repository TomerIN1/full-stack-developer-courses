// the strict inequality operator is basically the opposite of the strict equality operator. 

function testStrictNotEqual(val) {

    if (a !== b) { //now it is not going to convert the types. 

        return "Not Equal"
    }
    return "Equal"
}

console.log(testEqual(10, '10'));

